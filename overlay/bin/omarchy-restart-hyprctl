#!/bin/bash

# omarchy:summary=Regenerate the Niri theme include and reload Niri
# omarchy:hidden=true

# omarchy-theme-set runs this before its theme-set hook, so the border colors
# are regenerated here rather than from a hook that would fire after the reload.
omarchy-niri theme
exec niri msg action load-config-file
