--- a/bin/omarchy-bar-text-color
+++ b/bin/omarchy-bar-text-color
@@ -80,8 +80,8 @@
 [[ -f $background_path ]] || fallback
 
 if [[ -z $screen_size ]]; then
-  if omarchy-cmd-present hyprctl && omarchy-cmd-present jq; then
-    screen_size=$(hyprctl monitors -j 2>/dev/null | jq -r '.[0] | "\(.width)x\(.height)"' 2>/dev/null || true)
+  if omarchy-cmd-present niri && omarchy-cmd-present jq; then
+    screen_size=$(niri msg --json focused-output 2>/dev/null | jq -r '.logical | "\(.width)x\(.height)"' 2>/dev/null || true)
   fi
 fi
 [[ $screen_size =~ ^([0-9]+)x([0-9]+)$ ]] || fallback
