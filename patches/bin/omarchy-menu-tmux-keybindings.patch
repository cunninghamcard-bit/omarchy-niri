--- a/bin/omarchy-menu-tmux-keybindings
+++ b/bin/omarchy-menu-tmux-keybindings
@@ -127,7 +127,7 @@
 
 records=$(output_keybindings)
 
-monitor_height=$(hyprctl monitors -j 2>/dev/null | jq -r '.[] | select(.focused == true) | .height' 2>/dev/null)
+monitor_height=$(niri msg --json focused-output 2>/dev/null | jq -r '.logical.height' 2>/dev/null)
 
 if [[ ! $monitor_height =~ ^[0-9]+$ ]] || ((monitor_height <= 0)); then
   monitor_height=900
